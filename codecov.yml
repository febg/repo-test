coverage:
    status:
      project:
        default:
          # basic
          target: 0
          threshold: 0
          base: auto
          # advanced
      patch:
        default:
          # basic
          target: 0
          threshold: 0
          base: auto

      patch:
        default:
          # basic
          target: 0
          threshold: 0
          base: auto
      notify:
        require_ci_to_pass: no